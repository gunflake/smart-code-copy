{
    "name": "smart-copy-for-cli",
    "displayName": "smart copy for cli",
    "description": "Copy the current selection as a CLI-friendly reference. This extension converts the selected code range into a compact path + line range format like 'src/utils/test.py:37-42', or optionally 'src/utils/test.py:37-42' followed by a formatted Markdown code block. For example, when you select a Python function and run the command, the clipboard will contain the relative file path, the exact line numbers, and (optionally) the original code wrapped in ```python ... ``` so that you can paste it directly into issues, pull requests, documents, or chat tools.",
    "version": "1.0.0",
    "publisher": "lifebuddy",
    "repository": {
        "type": "git",
        "url": "https://github.com/your-name-or-org/smart-copy-for-cli.git"
    },
    "license": "MIT",
    "engines": {
        "vscode": "^1.85.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [],
    "main": "./out/extension.js",
    "contributes": {
        "commands": [
            {
                "command": "smartCodeCopy.copyPath",
                "title": "Smart Copy: Copy Path Only"
            },
            {
                "command": "smartCodeCopy.copyWithCode",
                "title": "Smart Copy: Copy Path and Code"
            }
        ],
        "keybindings": [
            {
                "command": "smartCodeCopy.copyPath",
                "key": "alt+shift+c",
                "mac": "alt+shift+c",
                "when": "editorTextFocus && editorHasSelection"
            },
            {
                "command": "smartCodeCopy.copyWithCode",
                "key": "ctrl+shift+alt+c",
                "mac": "cmd+shift+alt+c",
                "when": "editorTextFocus && editorHasSelection"
            }
        ],
        "menus": {
            "editor/context": [
                {
                    "command": "smartCodeCopy.copyPath",
                    "when": "editorHasSelection",
                    "group": "9_cutcopypaste"
                },
                {
                    "command": "smartCodeCopy.copyWithCode",
                    "when": "editorHasSelection",
                    "group": "9_cutcopypaste"
                }
            ]
        }
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "lint": "eslint src --ext ts"
    },
    "devDependencies": {
        "@types/vscode": "^1.85.0",
        "@types/node": "^20.10.0",
        "typescript": "^5.3.0"
    }
}